name: Rust

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: cargo build --all-features --all-targets --all
    - name: Run tests
      run: cargo test --all-features --all-targets --all
